import { AfterViewChecked, ElementRef } from '@angular/core';
export declare class FindLocationDirective implements AfterViewChecked {
    x: any;
    block: ElementRef;
    ngAfterViewChecked(): void;
}
