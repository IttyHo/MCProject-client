import { FindLocationDirective } from './find-location.directive';

describe('FindLocationDirective', () => {
  it('should create an instance', () => {
    const directive = new FindLocationDirective();
    expect(directive).toBeTruthy();
  });
});
